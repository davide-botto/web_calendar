import Vue from 'vue'
import Vuex from 'vuex'
import { deadlines } from "./deadlines.module";
Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    deadlines
  }
})
